<div class="productCard">
  <h2>{{ name }}</h2>
  <h3>£{{ price }}</h3>
  <img style="width: 100%;" src="{{ imageUrl }}" />
  <p>{{ description }}</p>

  <button><a href="/admin/edit-product/{{ _id }}">Edit</a></button>
  <form style="display: inline;" action="/admin/delete-product/{{ _id }}" method="POST">
    <input type="hidden" name="_csrf" value="{{ csrfToken }}" />
    <button type="submit">Delete</button>
  </form>
</div>